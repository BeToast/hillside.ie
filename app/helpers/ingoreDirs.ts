/* 
 * Dirnames declared in this array will be ingored when generating static pages.
 * directors subdirectores will be ingored as well.
 */

export const ignoreDirs: Set<string> = new Set(['Landing']);